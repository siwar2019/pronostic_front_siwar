import { makeStyles } from "@material-ui/core/styles";
export const useStyles = makeStyles((theme) => ({
  card: {
    backgroundColor: "#AEFF94 !important",
  },
  defaultCard: {
    backgroundColor: "#FFFFFF !important",
  },

  categoriesTitle: {
    letterSpacing: "2px",
  },
}));
